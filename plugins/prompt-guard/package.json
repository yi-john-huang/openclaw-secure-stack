{
  "name": "openclaw-prompt-guard",
  "version": "1.1.0",
  "description": "OpenClaw plugin that sanitizes tool results against indirect prompt injection",
  "main": "index.ts",
  "openclaw": {
    "hooks": ["tool_result_persist", "before_tool_call"]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
