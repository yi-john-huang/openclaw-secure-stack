# OpenClaw Gateway Environment Configuration
# Location: /etc/openclaw-secure-stack/openclaw.env
# Owner: root:openclaw (mode 0640)

# === LLM API Keys ===
# Set at least one provider. Configured during install.sh onboarding.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# === Gateway Authentication ===
# Token for authenticating the proxy → gateway connection.
# Auto-generated by install-native.sh (32 bytes, base64-encoded).
OPENCLAW_GATEWAY_TOKEN=

# === Plugin Configuration ===
# Enable prompt-guard plugin enforcement (blocks high-risk tool calls without governance headers).
PROMPT_GUARD_ENFORCEMENT=true

# Path to indirect injection rules (read by prompt-guard plugin).
INDIRECT_RULES_PATH=/opt/openclaw-secure-stack/config/indirect-injection-rules.json

# Path to quarantine list (read by prompt-guard plugin).
QUARANTINE_LIST_PATH=/opt/openclaw-secure-stack/config/quarantine-list.json

# === Optional: Telegram Bot Integration ===
# Set this to enable Telegram webhook relay (requires webhook setup).
TELEGRAM_BOT_TOKEN=
